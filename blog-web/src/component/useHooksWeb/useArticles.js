import axios from "axios";
import { useEffect, useState } from "react";

export default function useArticles() {
  const [list, setList] = useState([]);
  const [count, setCount] = use;
  return <div>useArticles</div>;
}
